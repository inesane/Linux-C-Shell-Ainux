#include "headers.h"
#include "bg.h"

void bg(char *inputs[], int args)
{
    if(args!=2)
    {
        fprintf(stderr, "ERROR: Incorrect number of arguments\n");
    }
    else
    {
        
    }
    
}