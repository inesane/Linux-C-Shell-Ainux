#include "headers.h"
#include "fg.h"

void fg(char *inputs[], int args)
{
    if(args!=2)
    {
        fprintf(stderr, "ERROR: Incorrect number of arguments\n");
    }
    else
    {
        
    }
    
}